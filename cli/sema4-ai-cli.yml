generated: '2026-07-21'
method: searched
source: https://github.com/Sema4AI/actions + https://sema4.ai/docs
cli:
  name: action-server
  package: sema4ai-action-server
  install:
  - pip install sema4ai-action-server
  - brew install sema4ai/tools/action-server
  - Download standalone binary from Sema4.ai CDN (Windows/Linux)
  description: >-
    CLI that bootstraps, runs, and exposes Action Server projects — turning
    Python @action functions into an OpenAPI-compatible REST API and MCP server.
  commands:
  - name: action-server new
    summary: Bootstrap a new action package project.
  - name: action-server start
    summary: Launch the Action Server locally (default port 8080); serves the web UI and /mcp endpoint.
    flags:
    - name: --expose
      summary: Generate a public URL for the local server (bearer-token protected).
  vscode_extension: https://marketplace.visualstudio.com/items?itemName=sema4ai.sema4ai
